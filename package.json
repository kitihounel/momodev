{
  "name": "momodev",
  "repository": {
    "type": "git",
    "url": "https://github.com/kitihounel/momodev"
  },
  "version": "1.0.0",
  "description": "NodeJs package to deal with MTN MoMo API",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --outDir build --target es2018 --module commonjs --moduleResolution node src/*",
    "build:watch": "tsc --outDir build --target es2018 --module commonjs --moduleResolution node --watch src/*",
    "clear": "rm -R build/*"
  },
  "author": "kitihounel",
  "license": "ISC",
  "dependencies": {
    "dotenv": "16.0.0",
    "follow-redirects": "1.14.9",
    "uuid": "8.3.2"
  },
  "devDependencies": {
    "@types/follow-redirects": "1.14.1",
    "@types/uuid": "8.3.4",
    "@types/node": "16.11.33",
    "typescript": "4.6.4"
  }
}
